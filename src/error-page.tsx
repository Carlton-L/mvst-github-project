/**
 * Error page which displays "404 Error" in case a Route cannot be found
 * @returns React element
 */
const ErrorPage = (): React.JSX.Element => {
  return <h1>404 Error</h1>;
};

export default ErrorPage;
